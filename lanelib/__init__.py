# SPDX-License-Identifier: MIT
"""Internals for the `lane` executable at the root of this repo.

Split by concern, not by layer: cow (kernel clone primitives), worktree (lane
lifecycle), memory (notes, anchors, staleness), review (model-in-the-loop).
"""
